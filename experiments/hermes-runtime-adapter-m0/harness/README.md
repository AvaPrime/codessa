# Harness

Reserved for the minimal Hermes adapter and conformance runner.

Implementation must remain outside the MCGL kernel and must consume existing Codessa runtime/assignment contracts rather than defining a parallel governance model.
